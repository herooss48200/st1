# AGROS R40.2 FIX NOTES

- BTC 15m SuperTrend remains the immutable final entry gate: LONG=UP/green, SHORT=DOWN/red. No rescue path.
- BTC EMA50/EMA200 transition corridor remains 0.15%; direction memory stays LONG/SHORT but new entries are locked inside the corridor.
- Strategy closed-candle scheduler corrected to 15 minutes (900000 ms).
- Test runs are isolated from persistent accounting-state files.
- LIVE Binance userTrades accounting now includes the recorded entry commission when the opening fill is outside the fetched trade window.
- Profit-floor call without an explicit current price no longer treats null as price 0; +1.00% -> +0.35% floor can activate correctly.
- Breadth remains a symmetric risk-size modifier; it cannot rescue an entry against the final BTC15 SuperTrend gate.
- Live limits remain 15 concurrent positions and 50 USDT maximum configured notional per position.
- Critical sizing fix: order-service now treats 50 USDT as a hard maximum, not a forced size; smaller risk-sized entries remain smaller.
