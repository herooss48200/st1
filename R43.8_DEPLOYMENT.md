# ST1 R43.8 — LIVE CORE + SHADOW

- CORE: R43.3 LONG, RSI `[28,30)`, taker buy ratio `[0.58,0.72]`; only this lane may reach the exchange order route in LIVE mode.
- SHADOW: RSI `[26,27)`, same flow band; it records virtual entries/exits and can never reach `enterPosition`.
- RSI `[27,28)` remains closed and all new SHORT entries remain closed.
- Default boot remains PAPER (`APP_MODE=paper`, `ENABLE_REAL_TRADING=false`). LIVE requires explicit environment variables and the preflight script.
- LIVE accounting uses `data/accounting-state-live.json` and `data/trade-snapshots-live.json`; existing PAPER history remains separate.
- SHADOW accounting uses `data/st1-r438-shadow-state.json`; rejected setups are appended to `data/st1-rejection-shadow.jsonl`.

Before deployment run `npm test`. Before every LIVE start run `npm run live:preflight` with Binance credentials available in the environment.
