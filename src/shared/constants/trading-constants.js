import config from '../../config/config.js';

// Runtime-tunable trading values live in .env and are parsed once by config.js.
// Protocol values and mathematical conversion factors intentionally remain code constants.
export const TRADING_CONSTANTS = {
  BTC_SYMBOL: config.BTC_SYMBOL,
  BTC_INTERVAL: config.BTC_TREND_INTERVAL,
  BTC_CANDLES_COUNT: config.BTC_TREND_CANDLE_LIMIT,

  SIMILARITY_THRESHOLD: config.SIMILARITY_THRESHOLD / 100,
  SIMILARITY_WINDOW_SIZE: config.SIMILARITY_WINDOW_SIZE,
  SIMILARITY_WEIGHTS: Object.freeze({ ...config.SIMILARITY_WEIGHTS }),

  MARKET_DATA_INTERVAL_1H: process.env.MARKET_DATA_INTERVAL_1H || '1h',
  MARKET_DATA_INTERVAL_1M: config.TRIGGER_INTERVAL,
  MARKET_DATA_FETCH_LIMIT: Number.parseInt(process.env.MARKET_DATA_FETCH_LIMIT, 10) || 1000,

  TREND_MA_PERIOD: Number.parseInt(process.env.TREND_MA_PERIOD, 10) || 20,
  TREND_THRESHOLD_UP: Number.parseFloat(process.env.TREND_THRESHOLD_UP) || 0.005,
  TREND_THRESHOLD_DOWN: Number.parseFloat(process.env.TREND_THRESHOLD_DOWN) || -0.005,

  TRIGGER_INTERVAL: config.TRIGGER_INTERVAL,
  BOLLINGER_PERIOD: config.BOLLINGER_PERIOD,
  TRIGGER_BOLLINGER_PERIOD: config.BOLLINGER_PERIOD,
  BOLLINGER_STD_DEV: config.BOLLINGER_STD_DEV,
  BOLLINGER_SOURCE: config.BOLLINGER_SOURCE,
  FULL_ALIGNMENT_EMA_PERIOD: config.FULL_ALIGNMENT_EMA_PERIOD,
  FULL_ALIGNMENT_EMA_TOUCH_ATR_MULTIPLIER: config.FULL_ALIGNMENT_EMA_TOUCH_ATR_MULTIPLIER,
  FULL_ALIGNMENT_EMA_REQUIRE_RECLAIM: config.FULL_ALIGNMENT_EMA_REQUIRE_RECLAIM,
  FULL_ALIGNMENT_EMA_REQUIRE_SLOPE: config.FULL_ALIGNMENT_EMA_REQUIRE_SLOPE,
  ATR_PERIOD: config.ATR_PERIOD,

  SNIPER_MAX_WAIT_MINUTES: Number.parseInt(process.env.SNIPER_MAX_WAIT_MINUTES, 10) || 30,
  SNIPER_MAX_SNIPERS: Number.parseInt(process.env.SNIPER_MAX_SNIPERS, 10) || 30,

  ATR_PERIOD: config.ATR_PERIOD,
  ATR_MULTIPLIER_SL: Number.parseFloat(process.env.ATR_SL_MULTIPLIER) || 1.5,
  ATR_MULTIPLIER_TRAILING: config.TRAILING_ATR_MULTIPLIER,
  BE_ACTIVATION_PERCENT: Number.parseFloat(process.env.BE_ACTIVATION_PERCENT) || 0.01,

  MAX_POSITIONS_PER_COIN: config.MAX_POSITIONS_PER_COIN,
  MAX_TOTAL_POSITIONS: config.MAX_POSITIONS,
  MAX_DAILY_LOSS_PERCENT: config.MAX_DAILY_LOSS_PERCENT,
  MAX_MONTHLY_LOSS_PERCENT: config.MAX_MONTHLY_LOSS_PERCENT,

  ORDER_RETRY_ATTEMPTS: config.ORDER_RETRY_ATTEMPTS,
  ORDER_RETRY_DELAY_MS: config.ORDER_RETRY_DELAY_MS,
  ORDER_TIMEOUT_MS: config.ORDER_TIMEOUT_MS,

  DATA_SYNC_INTERVAL_MS: Number.parseInt(process.env.DATA_SYNC_INTERVAL_MS, 10) || 5000,
  CACHE_TTL_SECONDS: Math.max(1, Math.round(config.CACHE_TTL / 1000)),

  RECOVERY_INTERVAL_MS: config.RECOVERY_INTERVAL_MS,
  RECOVERY_MAX_RETRIES: config.RECOVERY_MAX_RETRIES,
  RECOVERY_BACKOFF_MULTIPLIER: config.RECOVERY_BACKOFF_MULTIPLIER
};

export default Object.freeze(TRADING_CONSTANTS);
